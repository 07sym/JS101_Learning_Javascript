import React from 'react'
import ReactDOM from 'react-dom/client'
import Timer from '../../my-react-app/src/App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Timer />
  </React.StrictMode>,
)
